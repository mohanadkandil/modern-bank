import { useState } from "react"

import { close, logo, menu } from "../assets"

import { navLinks } from "../constants"

const Navbar = () => {
  return (
    <nav className="w-full flex py-6 items-center justify-center navbar"></nav>
  )
}

export default Navbar
